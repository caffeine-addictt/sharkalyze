{
  "private": false,
  "scripts": {
    "lint": "npm run lint --workspaces",
    "lint:fix": "npm run lint:fix --workspaces",
    "test": "npm run test --workspaces",
    "dev": "npx concurrently -c green,blue 'npm run dev --workspaces' 'make dev/python'",
    "pre-commit": "make lint"
  },
  "pre-commit": [
    "pre-commit"
  ],
  "workspaces": {
    "packages": [
      "client"
    ]
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "pre-commit": "^1.2.2"
  }
}
